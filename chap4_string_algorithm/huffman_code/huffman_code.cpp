#include "huffman_code.h"

Huffman_Encoding::Huffman_Encoding(const string& be){
    before_encoding = be;
    char char_log[before_encoding.length()];
    int count = 1;
    for(int i=0; i<before_encoding.length(); i++){
        if(
    }
}